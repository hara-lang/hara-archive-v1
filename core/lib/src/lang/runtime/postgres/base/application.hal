(ns lang.runtime.postgres.base.application
  (:config {:role :facade})
  (:require [postgres.application :as application]
            [std.foundation :as foundation]))

;; Compatibility owner for consumers that still use the historical runtime
;; namespace. The Vars are forwarded so the registry atom remains identical.
(foundation/intern-all postgres.application)
