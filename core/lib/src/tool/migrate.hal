(ns tool.migrate
  (:config {:role :facade})
  (:require [std.foundation :as f]
            [tool.migrate.common.transform]))

;; The public facade keeps the historical task surface while making the
;; registry-backed common transform directly available.
(f/intern-all tool.migrate.common.transform)
