(ns code.project.deploy.runtime
  (:require [tool.sh :as sh]))

(defn run
  "Runs one argv vector through the canonical capability-gated process boundary."
  ([argv]
   (run argv {}))
  ([argv options]
   (sh/require-success "Hara package command failed"
               (sh/run argv options))))
